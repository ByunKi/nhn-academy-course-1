package exercise.unit_4;

public class Exercise6 {

    private Exercise6() {

    }

    /*
     * byte b;
     * short s;
     * int i;
     * long l;
     * float f;
     * double d;
     * char c;
     * boolean b1, b2;
     * 
     * b + 10L → long
     * (b + i) * l → long
     * (b + i ) * l + f → long
     * s / f + Math.sin(f) → double
     * c == 'b' → boolean
     * l + 1.5f → float
     * i < 10 → boolean
     * b1 == (f >= 5.0) → boolean
     * b1 && !b2 → boolean
     */
}
